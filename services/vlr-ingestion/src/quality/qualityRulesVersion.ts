/** Bumping this signals that the quality-audit/reconciliation/curation rules changed, so a re-run against identical input data intentionally produces a new dataset version — see TASK-043 requirement 19 and `curate/curatedVersion.ts`. */
export const QUALITY_RULES_VERSION = "vlr-quality-rules@1.0.0";
